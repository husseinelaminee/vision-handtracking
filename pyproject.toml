[project]
name = "vision-handtracking"
version = "0.1.0"
requires-python = ">=3.10,<3.12"

[tool.setuptools.packages.find]
where = ["."]
exclude = ["manual_tests*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
norecursedirs = ["manual_tests"]
python_files = "test_*.py"